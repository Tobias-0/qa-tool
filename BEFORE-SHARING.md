# Secrets

`config.json` holds your Anthropic API key, Fizzy token, board IDs, and assignee IDs.

It is gitignored and is created locally when you save Settings. Never commit it.

If you zip this folder to share, delete `config.json` first.
